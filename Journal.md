# Journal
The Singleton made it easy for all parts of the program to share the same configuration without passing the object everywhere. The downside is that it creates global state and hidden dependencies, which can make testing harder. I think Singleton works well when there truly should only be one shared object, but dependency injection can be better when you want dependencies to be more visible and easier to test.